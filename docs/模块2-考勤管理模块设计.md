# 模块2: 考勤管理模块详细设计

## 1. 功能概述

考勤管理模块用于管理企业员工的日常考勤，包括打卡签到、请假申请、加班申请、调休申请、考勤统计等功能。

**注意**：移动端功能（打卡定位等）本次不实现，仅实现PC端功能。

## 2. 功能清单

| 功能点 | 说明 | 优先级 |
|--------|------|--------|
| 打卡签到 | PC端打卡，记录打卡时间、设备信息 | P1 |
| 考勤记录 | 个人考勤日历，按月/周查看，导出Excel | P1 |
| 请假申请 | 年假/事假/病假/婚假/产假，关联审批流程 | P1 |
| 加班申请 | 记录加班时长、原因，关联调休 | P1 |
| 调休申请 | 加班时长兑换调休，有效期管理 | P1 |
| 考勤统计 | 按部门/个人统计出勤率、请假天数、加班时长 | P2 |
| 考勤规则 | 配置上班时间、弹性时间、迟到/早退阈值、免打卡规则 | P2 |

## 3. 数据模型设计

### 3.1 考勤记录表 (attendance_record)

```sql
CREATE TABLE attendance_record (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    user_id BIGINT NOT NULL COMMENT '用户ID',
    record_date DATE NOT NULL COMMENT '考勤日期',
    check_in_time DATETIME COMMENT '签到时间',
    check_out_time DATETIME COMMENT '签退时间',
    check_in_device VARCHAR(100) COMMENT '签到设备信息',
    check_out_device VARCHAR(100) COMMENT '签退设备信息',
    check_in_location VARCHAR(255) COMMENT '签到位置（预留移动端）',
    check_out_location VARCHAR(255) COMMENT '签退位置',
    status VARCHAR(20) DEFAULT 'normal' COMMENT '状态: normal-正常, late-迟到, early_leave-早退, absent-缺卡, leave-请假',
    remark VARCHAR(500) COMMENT '备注',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_user_date (user_id, record_date),
    INDEX idx_record_date (record_date)
);
```

### 3.2 请假申请表 (leave_request)

```sql
CREATE TABLE leave_request (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    user_id BIGINT NOT NULL COMMENT '申请人ID',
    leave_type VARCHAR(20) NOT NULL COMMENT '请假类型: annual-年假, sick-病假, personal-事假, marriage-婚假, maternity-产假, paternity-陪产假, funeral-丧假',
    start_date DATE NOT NULL COMMENT '开始日期',
    end_date DATE NOT NULL COMMENT '结束日期',
    total_days DECIMAL(5,1) NOT NULL COMMENT '请假总天数',
    reason VARCHAR(500) COMMENT '请假原因',
    attachment_url VARCHAR(255) COMMENT '附件URL',
    status VARCHAR(20) DEFAULT 'pending' COMMENT '状态: pending-待审批, approved-已批准, rejected-已拒绝, cancelled-已取消',
    approval_instance_id VARCHAR(64) COMMENT '审批流程实例ID',
    approver_id BIGINT COMMENT '审批人ID',
    approved_at DATETIME COMMENT '审批时间',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_user_id (user_id),
    INDEX idx_status (status),
    INDEX idx_dates (start_date, end_date)
);
```

### 3.3 加班申请表 (overtime_request)

```sql
CREATE TABLE overtime_request (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    user_id BIGINT NOT NULL COMMENT '申请人ID',
    overtime_date DATE NOT NULL COMMENT '加班日期',
    start_time DATETIME NOT NULL COMMENT '开始时间',
    end_time DATETIME NOT NULL COMMENT '结束时间',
    duration_hours DECIMAL(5,1) NOT NULL COMMENT '加班时长（小时）',
    reason VARCHAR(500) COMMENT '加班原因',
    status VARCHAR(20) DEFAULT 'pending' COMMENT '状态: pending-待审批, approved-已批准, rejected-已拒绝, cancelled-已取消',
    approval_instance_id VARCHAR(64) COMMENT '审批流程实例ID',
    approver_id BIGINT COMMENT '审批人ID',
    approved_at DATETIME COMMENT '审批时间',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_user_id (user_id),
    INDEX idx_status (status),
    INDEX idx_overtime_date (overtime_date)
);
```

### 3.4 调休申请表 (compensatory_leave)

```sql
CREATE TABLE compensatory_leave (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    user_id BIGINT NOT NULL COMMENT '申请人ID',
    overtime_request_id BIGINT COMMENT '关联的加班申请ID',
    leave_date DATE NOT NULL COMMENT '调休日期',
    duration_hours DECIMAL(5,1) NOT NULL COMMENT '调休时长（小时）',
    status VARCHAR(20) DEFAULT 'pending' COMMENT '状态: pending-待审批, approved-已批准, rejected-已拒绝, cancelled-已取消',
    approval_instance_id VARCHAR(64) COMMENT '审批流程实例ID',
    approver_id BIGINT COMMENT '审批人ID',
    approved_at DATETIME COMMENT '审批时间',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_user_id (user_id),
    INDEX idx_status (status),
    INDEX idx_leave_date (leave_date)
);
```

### 3.5 加班调休余额表 (overtime_balance)

```sql
CREATE TABLE overtime_balance (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    user_id BIGINT NOT NULL COMMENT '用户ID',
    year INT NOT NULL COMMENT '年份',
    total_hours DECIMAL(5,1) DEFAULT 0 COMMENT '本年总加班时长',
    used_hours DECIMAL(5,1) DEFAULT 0 COMMENT '已使用调休时长',
    available_hours DECIMAL(5,1) DEFAULT 0 COMMENT '可用调休时长',
    expired_hours DECIMAL(5,1) DEFAULT 0 COMMENT '已过期时长',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    UNIQUE KEY uk_user_year (user_id, year)
);
```

### 3.6 考勤规则表 (attendance_rule)

```sql
CREATE TABLE attendance_rule (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    rule_name VARCHAR(50) NOT NULL COMMENT '规则名称',
    work_start_time TIME NOT NULL COMMENT '上班时间',
    work_end_time TIME NOT COMMENT '下班时间',
    flexible_minutes INT DEFAULT 0 COMMENT '弹性时间（分钟）',
    late_threshold_minutes INT DEFAULT 15 COMMENT '迟到阈值（分钟）',
    early_leave_threshold_minutes INT DEFAULT 15 COMMENT '早退阈值（分钟）',
    min_work_hours DECIMAL(3,1) DEFAULT 8.0 COMMENT '每日最低工作时长',
    is_default TINYINT(1) DEFAULT 0 COMMENT '是否为默认规则',
    enabled TINYINT(1) DEFAULT 1 COMMENT '是否启用',
    remark VARCHAR(255) COMMENT '备注',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);
```

### 3.7 年假余额表 (annual_leave_balance)

```sql
CREATE TABLE annual_leave_balance (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    user_id BIGINT NOT NULL COMMENT '用户ID',
    year INT NOT NULL COMMENT '年份',
    total_days DECIMAL(5,1) DEFAULT 0 COMMENT '本年可休年假天数',
    used_days DECIMAL(5,1) DEFAULT 0 COMMENT '已使用天数',
    available_days DECIMAL(5,1) DEFAULT 0 COMMENT '可用天数',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    UNIQUE KEY uk_user_year (user_id, year)
);
```

## 4. API设计

### 4.1 打卡签到

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /api/attendance/check-in | 签到 |
| POST | /api/attendance/check-out | 签退 |
| GET | /api/attendance/records | 考勤记录列表 |
| GET | /api/attendance/records/{id} | 考勤记录详情 |
| GET | /api/attendance/calendar | 考勤日历 |

### 4.2 请假管理

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /api/attendance/leave | 创建请假申请 |
| GET | /api/attendance/leave | 请假列表 |
| GET | /api/attendance/leave/{id} | 请假详情 |
| PUT | /api/attendance/leave/{id} | 修改请假申请 |
| DELETE | /api/attendance/leave/{id} | 取消请假申请 |
| GET | /api/attendance/leave/balance | 年假余额 |

### 4.3 加班管理

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /api/attendance/overtime | 创建加班申请 |
| GET | /api/attendance/overtime | 加班列表 |
| GET | /api/attendance/overtime/{id} | 加班详情 |
| DELETE | /api/attendance/overtime/{id} | 取消加班申请 |
| GET | /api/attendance/overtime/balance | 调休余额 |

### 4.4 调休管理

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /api/attendance/compensatory | 创建调休申请 |
| GET | /api/attendance/compensatory | 调休列表 |
| GET | /api/attendance/compensatory/{id} | 调休详情 |
| DELETE | /api/attendance/compensatory/{id} | 取消调休申请 |

### 4.5 考勤统计

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/attendance/statistics/personal | 个人考勤统计 |
| GET | /api/attendance/statistics/department | 部门考勤统计 |
| GET | /api/attendance/statistics/export | 导出考勤报表 |

### 4.6 考勤规则

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/attendance/rule | 获取考勤规则 |
| PUT | /api/attendance/rule | 更新考勤规则 |

## 5. 实体类设计

### AttendanceRecord
- id: Long
- userId: Long
- recordDate: LocalDate
- checkInTime: LocalDateTime
- checkOutTime: LocalDateTime
- checkInDevice: String
- checkOutDevice: String
- checkInLocation: String
- checkOutLocation: String
- status: AttendanceStatus (枚举: NORMAL, LATE, EARLY_LEAVE, ABSENT, LEAVE)
- remark: String

### LeaveRequest
- id: Long
- userId: Long
- leaveType: LeaveType (枚举: ANNUAL, SICK, PERSONAL, MARRIAGE, MATERNITY, PATERNITY, FUNERAL)
- startDate: LocalDate
- endDate: LocalDate
- totalDays: BigDecimal
- reason: String
- attachmentUrl: String
- status: RequestStatus
- approvalInstanceId: String
- approverId: Long
- approvedAt: LocalDateTime

### OvertimeRequest
- id: Long
- userId: Long
- overtimeDate: LocalDate
- startTime: LocalDateTime
- endTime: LocalDateTime
- durationHours: BigDecimal
- reason: String
- status: RequestStatus
- approvalInstanceId: String
- approverId: Long
- approvedAt: LocalDateTime

### CompensatoryLeave
- id: Long
- userId: Long
- overtimeRequestId: Long
- leaveDate: LocalDate
- durationHours: BigDecimal
- status: RequestStatus
- approvalInstanceId: String

### AttendanceRule
- id: Long
- ruleName: String
- workStartTime: LocalTime
- workEndTime: LocalTime
- flexibleMinutes: Integer
- lateThresholdMinutes: Integer
- earlyLeaveThresholdMinutes: Integer
- minWorkHours: BigDecimal
- isDefault: Boolean
- enabled: Boolean

## 6. 业务逻辑

### 6.1 签到/签退逻辑
1. 获取当前用户
2. 获取当前日期的考勤记录
3. 如果是签到：检查是否已签到，设置签到时间和设备信息
4. 如果是签退：检查是否已签退，设置签退时间和设备信息
5. 根据签到/签退时间和考勤规则，计算考勤状态

### 6.2 请假申请逻辑
1. 验证请假类型和日期
2. 检查年假余额（年假类型）
3. 创建请假申请记录
4. 触发审批流程（调用WorkflowService）
5. 返回申请结果

### 6.3 加班申请逻辑
1. 验证加班时间和时长
2. 计算加班时长（结束时间 - 开始时间）
3. 创建加班申请记录
4. 触发审批流程
5. 审批通过后，更新加班调休余额

### 6.4 调休申请逻辑
1. 验证调休日期和时长
2. 检查调休余额是否充足
3. 创建调休申请记录
4. 触发审批流程
5. 审批通过后，扣减调休余额

## 7. 待实现清单

- [ ] 实体类: AttendanceRecord, LeaveRequest, OvertimeRequest, CompensatoryLeave, AttendanceRule, OvertimeBalance, AnnualLeaveBalance
- [ ] Repository层
- [ ] Service层
- [ ] Controller层
- [ ] 单元测试
- [ ] 数据库迁移脚本
